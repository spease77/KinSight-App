name: kinsight_mobile
description: KinSight mobile — hands-free relationship intelligence
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  app_links: ^6.3.2
  porcupine_flutter: ^3.0.2
  permission_handler: ^11.3.1
  path_provider: ^2.1.4
  http: ^1.2.2
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/wake_word/
    - .env
